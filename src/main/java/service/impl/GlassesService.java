package service.impl;


import entity.Product;
import service.ProductService;

public class GlassesService implements ProductService {

    @Override
    public Product findById(String id) {

/*        DAOFactory factory = DAOFactory.getInstance();
        ProductDAO productDAO = factory.getApplianceDAO();
        productDAO.



        atalogue catalogue = productDAO.equals(d);
        Product product= productDAO.findById(criteria);

        // you may add your own code here

        return appliance;*/
        return null;
    }
}
